import { title } from "process";
import Success from "./_components/success";

export const metadata = {
  title: "FAI Cafe | Payment Success",
};

export default function SuccessPage() {
  return <Success />;
}
