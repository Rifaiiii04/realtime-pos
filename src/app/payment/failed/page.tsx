import { title } from "process";
import Failed from "./_components/failed";

export const metadata = {
  title: "FAI Cafe | Payment Failed",
};

export default function FailedPage() {
  return <Failed />;
}
