import { title } from "process";
import MenuManagement from "./_components/menu";

export const metadata = {
  title: "FAI Cafe | Menu Management",
};

export default function UserManagementPage() {
  return <MenuManagement />;
}
